import { Card, CardContent, CardMedia, Dialog, Popover, Skeleton, Typography } from '@mui/material'
import { Box } from '@mui/system';
import React from 'react'
import { RotatingLines } from 'react-loader-spinner';

const SearchResults = ({ loading, searchList, close }) => {


    return (
        <></>
    )
}

export default SearchResults